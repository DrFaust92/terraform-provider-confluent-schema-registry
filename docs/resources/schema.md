---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "schemaregistry_schema Resource - schemaregistry"
subcategory: ""
description: |-
  Manages a schema (and its versions) in the Schema Registry under a subject.
---

# schemaregistry_schema (Resource)

Manages a schema (and its versions) in the Schema Registry under a subject.

## Example Usage

```terraform
resource "schemaregistry_schema" "example" {
  subject = "example"
  schema = jsonencode({
    type = "record"
    name = "Example"
    fields = [
      {
        name = "id"
        type = "string"
      }
    ]
  })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `schema` (String) The schema string.
- `subject` (String) The subject related to the schema.

### Optional

- `compatibility` (String) The compatibility level of the subject.
- `reference` (Block List) The referenced schema list. (see [below for nested schema](#nestedblock--reference))

### Read-Only

- `id` (String) The subject of the schema.
- `schema_id` (Number) The ID of the schema.
- `version` (Number) The schema version.

<a id="nestedblock--reference"></a>
### Nested Schema for `reference`

Required:

- `name` (String) The referenced schema name.
- `subject` (String) The referenced schema subject.
- `version` (Number) The referenced schema version.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import schemaregistry_schema.example example
```
